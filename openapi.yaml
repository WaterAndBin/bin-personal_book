# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Greeter API
    version: 0.0.1
paths:
    /login:
        post:
            tags:
                - Greeter
            description: 登录
            operationId: Greeter_Login
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/api.user.v1.LoginParams'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.user.v1.LoginResult'
components:
    schemas:
        api.user.v1.LoginParams:
            type: object
            properties:
                account:
                    type: string
                password:
                    type: string
        api.user.v1.LoginResult:
            type: object
            properties:
                code:
                    type: string
                message:
                    type: string
                token:
                    type: string
tags:
    - name: Greeter
